<!DOCTYPE html>
<html>
    <head> 
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" crossorigin="anonymous">
        <title>Ostoslista</title>
    </head>
    <body>
        <div class="container">
            <h2 class="text-center mt-5 mb-3">Kirjaudu</h2>
            <form method="POST" action="/kirjaudu" class="needs-validation" novalidate>
                <input type="text" name="tunnus" placeholder="Käyttäjänimi" class="form-control w-50 float-left" required>
                <input type="password" name="salasana" placeholder="Salasana" class="form-control w-50" required>
                <input type="submit" value="Kirjaudu" name="kirjaudu" class="btn btn-primary w-100 my-3">
            </form>
            
            <%if(virhe !=""){%>
            <div class="alert alert-danger">
                <%=virhe%>
            </div>
            <%}%>
            
            <h2 class="text-center my-3">Tai rekisteröidy</h2>
            
            <form method="POST" action="/luoKayttaja" class="needs-validation" novalidate>
                <input type="text" name="sPosti" placeholder="Sähköposti"  class="form-control w-100" required>
                <input type="text" name="tunnus" placeholder="Käyttäjänimi" class="form-control w-50 float-left mt-2" required>
                <input type="password" name="salasana" placeholder="Salasana"  class="form-control w-50 mt-2" required>
                <input type="submit" value="Luo" name="kirjaudu" class="btn btn-primary w-100 mt-3">
            </form>
        </div>
    </body>
    
    <script>
 //https://getbootstrap.com/docs/4.2/components/forms/#validation
    (function() {
      'use strict';
      window.addEventListener('load', function() {
        // Fetch all the forms we want to apply custom Bootstrap validation styles to
        var forms = document.getElementsByClassName('needs-validation');
        // Loop over them and prevent submission
        var validation = Array.prototype.filter.call(forms, function(form) {
          form.addEventListener('submit', function(event) {
            if (form.checkValidity() === false) {
              event.preventDefault();
              event.stopPropagation();
            }
            form.classList.add('was-validated');
          }, false);
        });
      }, false);
    })();
</script>
</html>