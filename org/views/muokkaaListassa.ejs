<!DOCTYPE html>
<html>
    <head> 
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" crossorigin="anonymous">
        <title>Ostoslista</title>
    </head>
    <body>
        <div class="container">
            <h1 class="mb-4">Ostoslista <%=tiedot[0].listanNimi%></h1>
            
            <table class="table table-bordered">
                <tbody>
                    
                    <%tiedot.forEach((tieto)=>{%>
                        <%if(tieto.id == muokattava){%>
                    <tr>
                        <form method="POST" action="/tallennaMuokkaus">
                            <td>
                                <%if(tieto.ostettu == 1){%>
                                    <input type="checkbox" name="ostettu" class="form-control" checked>
                                <%}
                                else{%>
                                    <input type="checkbox" name="ostettu" class="form-control">
                                <%}%>
                            </td>
                            <td>
                                <input type="text" name="sisalto" class="form-control" value="<%=tieto.sisalto%>">
                            </td>
                            <td>
                                <input type="hidden" name="id" value="<%=tieto.id%>">
                                <input type="submit" value="Tallenna" class="btn btn-primary">
                            </td>
                            </form>
                        
                            <td>
                                <a href="/lista/<%=tieto.listanNimi%>" class="btn btn-secondary">Peruuta</a>
                            </td>
                    </tr>
                    <%}
                    else{%>
                        <tr>
                            <td>
                                <%if(tieto.ostettu == 1){%>
                                    <span class="fas fa-check"></span>
                                <%}
                                else{%>
                                    <span class="fas fa-times"></span>
                                <%}%>
                            </td>
                            <td>
                                <%=tieto.sisalto%>
                            </td>
                            <td></td>
                            <td></td>
                        </tr>
                    <%}});%>
                </tbody>
            </table>
        </div>
    </body>
</html>